// @ts-nocheck
"use client";import {Share2} from "lucide-react";export function ShareButton({title}:{title:string}){async function share(){const url=window.location.href;if(typeof navigator!=="undefined"&&navigator.share)await navigator.share({title,url});else if(typeof navigator!=="undefined"&&navigator.clipboard)await navigator.clipboard.writeText(url)}return <button className="cta secondary" onClick={share}><Share2 size={16}/>Compartilhar</button>}
