"use client";import {useRef,useState} from "react";import {Play,Pause} from "lucide-react";
export function GospelAudioButton({audioUrl,text}:{audioUrl?:string|null;text:string}){const audioRef=useRef<HTMLAudioElement|null>(null);const [playing,setPlaying]=useState(false);function toggle(){if(audioUrl){const a=audioRef.current;if(!a)return;if(a.paused){a.play();setPlaying(true)}else{a.pause();setPlaying(false)}return}if("speechSynthesis" in window){if(window.speechSynthesis.speaking){window.speechSynthesis.cancel();setPlaying(false)}else{const u=new SpeechSynthesisUtterance(text);u.lang="pt-BR";u.rate=.9;u.onend=()=>setPlaying(false);window.speechSynthesis.speak(u);setPlaying(true)}}}return <><button className="moment-audio" onClick={toggle}><span className="moment-audio-icon">{playing?<Pause size={22}/>:<Play size={22}/>}</span>{playing?"Pausar Evangelho":"Ouvir Evangelho"}</button>{audioUrl&&<audio ref={audioRef} src={audioUrl} onEnded={()=>setPlaying(false)} preload="none"/>}</>}
