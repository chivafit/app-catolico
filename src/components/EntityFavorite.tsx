"use client";import Link from "next/link";import {useEffect,useState} from "react";import {Heart} from "lucide-react";import {useAuth} from "@/components/AuthProvider";import {getSupabase} from "@/lib/supabase";export function EntityFavorite({type,id}:{type:string;id:string}){const {user}=useAuth();const [on,setOn]=useState(false);useEffect(()=>{if(!user)return;const userId=user.id;getSupabase().from("favorites").select("entity_id").eq("user_id",userId).eq("entity_type",type).eq("entity_id",id).maybeSingle().then(({data})=>setOn(!!data))},[user?.id,type,id]);if(!user)return <Link className="cta secondary" href="/entrar">Entrar para salvar</Link>;const userId=user.id;async function toggle(){const s=getSupabase();if(on)await s.from("favorites").delete().eq("user_id",userId).eq("entity_type",type).eq("entity_id",id);else await s.from("favorites").insert({user_id:userId,entity_type:type,entity_id:id});setOn(!on)}return <button className="cta secondary" onClick={toggle}><Heart size={16} fill={on?"currentColor":"none"}/>{on?"Salvo":"Favoritar"}</button>}
