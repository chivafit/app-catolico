"use client";import {useEffect,useState} from "react";import {getSupabase} from "@/lib/supabase";import {CalendarDays,ShieldCheck,MapPin,ExternalLink} from "lucide-react";
const months=["","Janeiro","Fevereiro","Março","Abril","Maio","Junho","Julho","Agosto","Setembro","Outubro","Novembro","Dezembro"];
export function RecurringEvents(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{getSupabase().from("recurring_events").select("*").eq("active",true).order("month").order("day").then(({data})=>setItems(data||[]))},[]);if(!items.length)return null;return <section style={{marginTop:26}}><div className="section-head"><div><div className="eyebrow">CALENDÁRIO DE PIUMHI</div><h2>Eventos e tradições que voltam todo ano</h2></div></div><div className="grid">{items.map(x=><article className="card" key={x.id}><div style={{display:"flex",justifyContent:"space-between",gap:10,alignItems:"flex-start"}}><div className="iconbox"><CalendarDays/></div>{x.verified&&<span className="tag"><ShieldCheck size={12}/> Verificado</span>}</div><h3 style={{marginTop:12}}>{x.title}</h3><p className="tiny" style={{fontWeight:800}}>{x.day?`${String(x.day).padStart(2,"0")} de ${months[x.month]}`:(x.month?months[x.month]:"Data variável")}</p>{x.date_note&&<p className="tiny muted">{x.date_note}</p>}<p className="muted">{x.description}</p>{x.location&&<p className="tiny muted" style={{display:"flex",gap:6,alignItems:"center"}}><MapPin size={14}/>{x.location}</p>}{x.source_url&&<a className="tiny" href={x.source_url} target="_blank" rel="noreferrer" style={{display:"inline-flex",gap:5,alignItems:"center"}}>Fonte oficial <ExternalLink size={12}/></a>}</article>)}</div></section>}
