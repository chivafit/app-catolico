import Link from "next/link";import {AppShell} from "@/components/AppShell";import {brl,getCampaigns,getEvents,getPilgrimages,shortDate} from "@/lib/data";
export default async function Explorar(){const [events,campaigns,pilgrimages]=await Promise.all([getEvents(),getCampaigns(),getPilgrimages()]);return <AppShell><div className="page-title"><div className="eyebrow">Descobrir</div><h1>Há muito mais para viver na fé.</h1><p className="muted">Encontre experiências, encontros e iniciativas que aproximam você de pessoas e lugares com o mesmo propósito.</p></div><section className="grid">{events.slice(0,2).map(e=><div className="card" key={e.id}><span className="tag">Encontro</span><h2 style={{marginTop:18}}>{e.title}</h2><p className="muted">{shortDate(e.starts_at)} · {e.location||"local a confirmar"}</p><p className="product-price">{e.price_cents?brl(e.price_cents):"Entrada gratuita"}</p></div>)}{pilgrimages.slice(0,1).map(p=><div className="card" key={p.id}><span className="tag">Peregrinação</span><h2 style={{marginTop:18}}>{p.destination}</h2><p className="muted">{shortDate(p.starts_at)} · saída de {p.origin_city||"local a confirmar"}</p><p className="product-price">{brl(p.price_cents)}</p><Link className="cta" href="/peregrinacoes">Conhecer jornada →</Link></div>)}</section><div className="section-head"><div><div className="eyebrow">Comunidade</div><h2>Faça parte de algo maior.</h2></div><Link href="/campanhas" className="muted">Todas as campanhas →</Link></div>{campaigns.slice(0,1).map(c=>{const pct=Math.min(100,Math.round(c.raised_cents/Math.max(1,c.goal_cents||1)*100));return <div className="card" key={c.id}><span className="tag">Campanha verificada</span><h2 style={{marginTop:18}}>{c.title}</h2><p className="muted">{c.description}</p><div className="progress"><span style={{width:`${pct}%`}}/></div><p><b>{pct}% da meta alcançada</b></p><Link href="/campanhas" className="cta">Conhecer campanha →</Link></div>})}</AppShell>}
