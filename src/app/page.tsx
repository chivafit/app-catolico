import {AppShell} from "@/components/AppShell";import {HomeReference} from "@/components/HomeReference";import {WelcomeGate} from "@/components/WelcomeGate";import {getEvents,getTodayDevotional} from "@/lib/data";
export default async function HomePage(){const [d,events]=await Promise.all([getTodayDevotional(),getEvents()]);return <WelcomeGate><AppShell hideHeader><HomeReference devotional={d} event={events[0]||null}/></AppShell></WelcomeGate>}
