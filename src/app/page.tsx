import Link from "next/link";import {AppShell} from "@/components/AppShell";import {HomePersonalized} from "@/components/HomePersonalized";import {WelcomeGate} from "@/components/WelcomeGate";import {BookOpen,Heart,Bus,CalendarDays,ShoppingBag,Church} from "lucide-react";import {brl,getCampaigns,getEvents,getPilgrimages,getProducts,getTodayDevotional,shortDate} from "@/lib/data";
export default async function HomePage(){const [d,events,campaigns,pilgrimages,products]=await Promise.all([getTodayDevotional(),getEvents(),getCampaigns(),getPilgrimages(),getProducts()]);return <WelcomeGate><AppShell><HomePersonalized/><section className="hero"><div className="card hero-card"><div><div className="eyebrow">Momento Diário</div><h1>{d?.title||"Eu estou convosco todos os dias."}</h1><p className="quote">{d?.scripture_reference||"Liturgia do dia"}</p></div><Link className="cta" href="/momento"><BookOpen size={17}/> Ler agora</Link></div></section><div className="mobile-quick"><Link className="quick" href="/igreja"><Church/>Minha Igreja</Link><Link className="quick" href="/eventos"><CalendarDays/>Eventos</Link><Link className="quick" href="/peregrinacoes"><Bus/>Peregrinações</Link><Link className="quick" href="/campanhas"><Heart/>Campanhas</Link><Link className="quick" href="/loja"><ShoppingBag/>Loja</Link><Link className="quick" href="/livros"><BookOpen/>Livraria</Link></div><div className="section-head"><h2>Descobrir</h2><Link href="/explorar" className="muted">Ver tudo</Link></div><section className="grid">{events.slice(0,1).map(e=><Link href="/eventos" className="card" key={e.id}><CalendarDays size={20}/><h3 style={{marginTop:12}}>{e.title}</h3><p className="tiny muted">{shortDate(e.starts_at)}</p></Link>)}{pilgrimages.slice(0,1).map(p=><Link href="/peregrinacoes" className="card" key={p.id}><Bus size={20}/><h3 style={{marginTop:12}}>{p.destination}</h3><p className="tiny muted">{shortDate(p.starts_at)}</p></Link>)}{campaigns.slice(0,1).map(c=><Link href="/campanhas" className="card" key={c.id}><Heart size={20}/><h3 style={{marginTop:12}}>{c.title}</h3><p className="tiny muted">Campanha da comunidade</p></Link>)}{products.slice(0,1).map(p=><Link href={`/loja/${p.slug}`} className="card" key={p.id}><ShoppingBag size={20}/><h3 style={{marginTop:12}}>{p.name}</h3><p className="product-price">{brl(p.price_cents)}</p></Link>)}</section></AppShell></WelcomeGate>}
