"use client";import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import {AppShell} from "@/components/AppShell";import {useAuth} from "@/components/AuthProvider";import {getSupabase} from "@/lib/supabase";
const topics=["Momento Diário","Minha Paróquia","Eventos","Peregrinações","Livros","Campanhas"];
export default function Configuracoes(){const {user,profile,loading,refreshProfile}=useAuth();const router=useRouter();const [phone,setPhone]=useState(""),[prefs,setPrefs]=useState<Record<string,boolean>>({}),[push,setPush]=useState(true),[email,setEmail]=useState(true),[message,setMessage]=useState("");useEffect(()=>{if(!loading&&!user)router.replace("/entrar?next=/configuracoes");if(profile){setPhone(profile.phone||"");setPrefs(profile.preferences||{});setPush(profile.preferences?.notifications_push!==false);setEmail(profile.preferences?.notifications_email!==false)}},[user,profile,loading,router]);if(loading||!user)return <AppShell><div className="card">Carregando configurações…</div></AppShell>;const userId=user.id;async function save(){const s=getSupabase();const next={...prefs,notifications_push:push,notifications_email:email};const {error}=await s.from("profiles").update({phone:phone||null,preferences:next}).eq("id",userId);setMessage(error?error.message:"Preferências salvas.");if(!error)await refreshProfile()}async function exportData(){const s=getSupabase();const [p,f,m,b,o]=await Promise.all([s.from("profiles").select("*").eq("id",userId).maybeSingle(),s.from("favorites").select("*").eq("user_id",userId),s.from("devotional_completions").select("*").eq("user_id",userId),s.from("bookings").select("*").eq("user_id",userId),s.from("orders").select("*").eq("user_id",userId)]);const blob=new Blob([JSON.stringify({profile:p.data,favorites:f.data||[],devotional_completions:m.data||[],bookings:b.data||[],orders:o.data||[]},null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="agora-fide-meus-dados.json";a.click();URL.revokeObjectURL(a.href)}async function requestDeletion(){if(!confirm("Solicitar exclusão da sua conta e dados?"))return;const {error}=await getSupabase().from("account_deletion_requests").insert({user_id:userId});setMessage(error?error.message:"Solicitação de exclusão registrada.")}
return <AppShell><div className="page-title"><div className="eyebrow">Conta</div><h1>Configurações</h1><p className="muted">Controle seus dados e o que deseja acompanhar.</p></div><div className="card"><label className="field">Telefone opcional<input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="(00) 00000-0000" inputMode="tel"/></label><h3>Interesses</h3><div className="chips" style={{marginTop:10}}>{topics.map(t=><button key={t} className={`pill chip-button ${prefs[t]?"selected":""}`} onClick={()=>setPrefs({...prefs,[t]:!prefs[t]})}>{prefs[t]?"✓ ":""}{t}</button>)}</div><h3 style={{marginTop:22}}>Notificações</h3><label className="choice"><span>Notificações no app<small>Avisos e novidades relevantes</small></span><input type="checkbox" checked={push} onChange={e=>setPush(e.target.checked)}/></label><label className="choice"><span>Notificações por e-mail<small>Resumo e comunicações importantes</small></span><input type="checkbox" checked={email} onChange={e=>setEmail(e.target.checked)}/></label><button className="cta" onClick={save}>Salvar preferências</button>{message&&<p className="notice-inline">{message}</p>}</div><div className="section-head"><h2>Privacidade e LGPD</h2></div><div className="card list"><div className="row"><span><b>Exportar meus dados</b><small>Baixe uma cópia em JSON</small></span><button className="cta secondary" onClick={exportData}>Exportar</button></div><div className="row"><span><b>Excluir minha conta</b><small>Registra a solicitação para remoção definitiva</small></span><button className="cta secondary" onClick={requestDeletion}>Solicitar</button></div></div></AppShell>}
