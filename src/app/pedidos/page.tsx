"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {AppShell} from "@/components/AppShell";import {useAuth} from "@/components/AuthProvider";import {getSupabase} from "@/lib/supabase";
const brl=(c:number)=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(c/100);
export default function Pedidos(){const {user,loading}=useAuth();const [rows,setRows]=useState<any[]>([]);const [busy,setBusy]=useState(true);useEffect(()=>{if(loading)return;if(!user){setBusy(false);return}getSupabase().from("orders").select("id,status,total_cents,created_at").eq("user_id",user.id).order("created_at",{ascending:false}).then(({data})=>{setRows(data||[]);setBusy(false)})},[user,loading]);return <AppShell><div className="page-title"><h1>Meus Pedidos</h1><p className="muted">Acompanhe suas compras no Ágora Fide.</p></div>{busy?<div className="card">Carregando…</div>:!user?<div className="card"><h2>Entre para ver seus pedidos</h2><Link className="cta" href="/entrar?next=/pedidos">Entrar</Link></div>:rows.length?<div className="card list">{rows.map(x=><div className="row" key={x.id}><div><b>Pedido #{String(x.id).slice(0,8)}</b><small>{new Date(x.created_at).toLocaleDateString("pt-BR")} · {x.status}</small></div><strong>{brl(x.total_cents||0)}</strong></div>)}</div>:<div className="card"><h2>Nenhum pedido ainda</h2><p className="muted">Quando você concluir uma compra, ela aparecerá aqui.</p><Link className="cta" href="/loja">Ir para a loja</Link></div>}</AppShell>}
