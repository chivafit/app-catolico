import {notFound} from "next/navigation";import {AppShell} from "@/components/AppShell";import {ProductReference} from "@/components/ProductReference";import {getSupabase} from "@/lib/supabase";import "@/app/product-reference.css";
export default async function Produto({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const {data:p}=await getSupabase().from("products").select("id,name,description,price_cents,stock,image_url,category").eq("slug",slug).eq("active",true).maybeSingle();if(!p)notFound();return <AppShell hideHeader hideNav><ProductReference p={p}/></AppShell>}
