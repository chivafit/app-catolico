// @ts-nocheck
"use client";
import "./peregrinacoes.css";import Link from "next/link";import {useEffect,useMemo,useState} from "react";import {AppShell} from "@/components/AppShell";import {EntityFavorite} from "@/components/EntityFavorite";import {ShareButton} from "@/components/ShareButton";import {getSupabase} from "@/lib/supabase";import {Bus,Plane,MapPin,CalendarDays,ShieldCheck,Clock3,ArrowRight} from "lucide-react";
const brl=(c:number|null)=>c==null?"Sob consulta":new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(c/100);
const fallback="https://images.unsplash.com/photo-1548013146-72479768bada?auto=format&fit=crop&w=1200&q=82";const imgFor=(d:any)=>d?.hero_image_url||d?.image_url||fallback;
export default function Peregrinacoes(){const [trips,setTrips]=useState<any[]>([]),[destinations,setDestinations]=useState<any[]>([]),[origin,setOrigin]=useState(""),[destination,setDestination]=useState(""),[transport,setTransport]=useState("");useEffect(()=>{const s=getSupabase();Promise.all([s.from("pilgrimages").select("*,travel_organizers(name,verified),pilgrimage_destinations(name,slug,hero_image_url)").eq("status","published").order("starts_at"),s.from("pilgrimage_destinations").select("*").eq("active",true).order("featured",{ascending:false}).order("name")]).then(([a,b])=>{setTrips(a.data||[]);setDestinations(b.data||[])})},[]);const origins=useMemo(()=>Array.from(new Set(trips.map(x=>x.origin_city).filter(Boolean))),[trips]);const list=trips.filter(x=>(!origin||x.origin_city===origin)&&(!destination||x.destination_id===destination)&&(!transport||x.transport_type===transport));return <AppShell><div className="pilg"><section className="pilg-hero"><span className="pilg-kicker">PEREGRINAÇÕES</span><h1>Para onde sua fé te leva?</h1><p>Descubra destinos, compare saídas e acompanhe novas oportunidades de peregrinação em uma experiência feita para inspirar e facilitar sua jornada.</p></section><div className="pilg-search"><select value={origin} onChange={e=>setOrigin(e.target.value)}><option value="">Cidade de saída</option>{origins.map((x:any)=><option key={x}>{x}</option>)}</select><select value={destination} onChange={e=>setDestination(e.target.value)}><option value="">Destino</option>{destinations.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><select value={transport} onChange={e=>setTransport(e.target.value)}><option value="">Transporte</option><option value="bus">Ônibus</option><option value="air">Aéreo</option></select></div><div className="pilg-title"><h2>Viagens disponíveis</h2><span>{list.length} opções</span></div>{list.length?<section className="pilg-trips">{list.map(t=>{const organizer=Array.isArray(t.travel_organizers)?t.travel_organizers[0]:t.travel_organizers;const dest=Array.isArray(t.pilgrimage_destinations)?t.pilgrimage_destinations[0]:t.pilgrimage_destinations;return <article className="pilg-trip" key={t.id}><Link href={`/peregrinacoes/${dest?.slug||"aparecida"}?trip=${t.id}`} className="pilg-trip-link"><div className="pilg-trip-img" style={{backgroundImage:`url(${t.image_url||imgFor(dest||t)})`}}>{organizer?.verified&&<span className="pilg-trip-badge"><ShieldCheck size={11}/> Verificada</span>}</div><div className="pilg-trip-body"><h3>{t.title||t.destination}</h3><div className="pilg-meta"><span><MapPin size={13}/>{t.destination}</span><span><CalendarDays size={13}/>{new Date(t.starts_at).toLocaleDateString("pt-BR")}</span>{t.duration_days&&<span><Clock3 size={13}/>{t.duration_days} dias</span>}<span>{t.transport_type==="air"?<Plane size={13}/>:<Bus size={13}/>} {t.transport_type==="air"?"Aéreo":"Ônibus"}</span></div><div className="pilg-price-row"><div><div className="pilg-price">{brl(t.price_cents)}</div><div className="pilg-spots">{t.available_spots==null?"Vagas a confirmar":`${t.available_spots} vagas disponíveis`} {t.origin_city?`• saída de ${t.origin_city}`:""}</div></div><ArrowRight size={20}/></div></div></Link><div className="pilg-actions"><EntityFavorite type="pilgrimage" id={t.id}/><ShareButton title={t.title||t.destination}/></div></article>})}</section>:<div className="pilg-empty">Ainda não há viagens publicadas com esses filtros. Explore os destinos abaixo e acompanhe as próximas saídas.</div>}<div className="pilg-title"><h2>Destinos que inspiram</h2><span>Explore antes de escolher</span></div><section className="pilg-destinations">{destinations.map(d=><Link href={`/peregrinacoes/${d.slug}`} className="pilg-dest" key={d.id} style={{backgroundImage:`url(${imgFor(d)})`}}><div className="pilg-dest-copy"><span className="pilg-dest-tag">{d.category==="international"?"Internacional":"Brasil"}</span><h3>{d.name}</h3><p>{d.region?`${d.region} • `:""}{d.country}</p><span className="pilg-dest-open">Conhecer destino <ArrowRight size={13}/></span></div></Link>)}</section><div className="pilg-inspire"><strong>Uma viagem pode começar muito antes da partida.</strong><p>Conheça cada destino, sua espiritualidade, pontos de peregrinação e viagens disponíveis. A reserva e o pagamento serão feitos dentro do Vinde.</p></div></div></AppShell>}
